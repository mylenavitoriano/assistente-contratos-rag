services:
  backend:
    build:
      target: development
    environment:
      NODE_ENV: development
      LOG_LEVEL: ${LOG_LEVEL:-debug}
    volumes:
      - ./backend/src:/app/src
      - ./data:/app/data:ro
      - model_cache:/app/.model-cache
